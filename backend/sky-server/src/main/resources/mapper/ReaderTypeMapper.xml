<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.ReaderTypeMapper">

    <select id="listEnabled" resultType="com.sky.entity.ReaderType">
        SELECT
            `id`,
            `name`,
            `max_borrow` AS `maxBorrow`,
            `borrow_days` AS `borrowDays`,
            `max_renew` AS `maxRenew`,
            `description`,
            `status`,
            `create_time` AS `createTime`,
            `update_time` AS `updateTime`,
            `create_user` AS `createUser`,
            `update_user` AS `updateUser`,
            `is_deleted` AS `isDeleted`
        FROM `reader_type`
        WHERE `is_deleted` = 0
          AND `status` = 1
        ORDER BY `id` ASC
    </select>

    <select id="getById" parameterType="long" resultType="com.sky.entity.ReaderType">
        SELECT
            `id`,
            `name`,
            `max_borrow` AS `maxBorrow`,
            `borrow_days` AS `borrowDays`,
            `max_renew` AS `maxRenew`,
            `description`,
            `status`,
            `create_time` AS `createTime`,
            `update_time` AS `updateTime`,
            `create_user` AS `createUser`,
            `update_user` AS `updateUser`,
            `is_deleted` AS `isDeleted`
        FROM `reader_type`
        WHERE `is_deleted` = 0
          AND `status` = 1
          AND `id` = #{id}
        LIMIT 1
    </select>

</mapper>

